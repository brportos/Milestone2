[tool.poetry]
name = "ex1"
version = "0.1.0"
description = "A python module demonstrates package management usingoetry."
authors = ["brportos"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pandas = ">=2.0.0"
numpy = "^1.24"
matplotlib = "3.7.2"
requests = "2.31.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
